name: Python application

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.password }}" | docker login -u "hoshen12" --password-stdin 
        docker image build -t hoshen12/maor:python1 .
        docker push hoshen12/maor:python1
